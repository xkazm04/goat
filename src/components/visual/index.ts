/**
 * Visual Components - Public Exports
 *
 * Unified export for all visual primitives (depth + decorations).
 *
 * Usage:
 *   import { Elevated, Surface, Glow, Shimmer, GradientBorder } from '@/components/visual';
 *   import { ELEVATION, GLOW_PRESET, GRADIENT_PRESETS } from '@/components/visual';
 */

// Depth components and tokens
export * from './depth';

// Decoration components
export * from './decorations';
