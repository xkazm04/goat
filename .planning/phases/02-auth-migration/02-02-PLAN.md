---
phase: 02-auth-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/auth/AuthModal.tsx
  - src/components/auth/AuthPrompt.tsx
  - src/components/auth/UserMenu.tsx
  - src/components/auth/index.ts
  - src/app/layout.tsx
  - src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "User sees a 'Continue with Google' modal when they choose to sign in"
    - "After completing a ranking, user sees a dismissible sign-up nudge"
    - "Authenticated user sees their avatar in the header with a sign-out option"
    - "After sign-up, user sees 'Welcome! Your rankings are saved.' toast"
    - "Guest can dismiss all auth prompts and continue ranking freely"
  artifacts:
    - path: "src/components/auth/AuthModal.tsx"
      provides: "Google OAuth modal overlay"
      min_lines: 40
    - path: "src/components/auth/AuthPrompt.tsx"
      provides: "Post-completion dismissible sign-up nudge"
      min_lines: 20
    - path: "src/components/auth/UserMenu.tsx"
      provides: "Avatar dropdown with sign-out"
      min_lines: 25
  key_links:
    - from: "src/components/auth/AuthModal.tsx"
      to: "src/hooks/use-auth-user.ts"
      via: "useAuthUser().signInWithGoogle"
      pattern: "signInWithGoogle"
    - from: "src/components/auth/AuthPrompt.tsx"
      to: "src/components/auth/AuthModal.tsx"
      via: "Opens AuthModal on click"
      pattern: "AuthModal"
    - from: "src/components/auth/UserMenu.tsx"
      to: "src/hooks/use-auth-user.ts"
      via: "useAuthUser().signOut"
      pattern: "signOut"
---

<objective>
Build the auth UI components — sign-in modal, post-completion nudge, and user menu.

Purpose: Give users a way to sign in with Google, get prompted after completing a ranking, see their identity in the header, and sign out. This completes the user-facing auth experience.
Output: AuthModal, AuthPrompt, UserMenu components wired into the app.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-migration/02-CONTEXT.md
@.planning/phases/02-auth-migration/02-RESEARCH.md
@.planning/phases/02-auth-migration/02-01-SUMMARY.md

@src/hooks/use-auth-user.ts
@src/hooks/use-toast.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthModal, AuthPrompt, and UserMenu components</name>
  <files>
    src/components/auth/AuthModal.tsx,
    src/components/auth/AuthPrompt.tsx,
    src/components/auth/UserMenu.tsx,
    src/components/auth/index.ts
  </files>
  <action>
1. Create `src/components/auth/AuthModal.tsx`:
   - Modal overlay (use Radix Dialog or a simple fixed overlay with backdrop — match project patterns)
   - Single "Continue with Google" button with Google icon SVG + text, styled to match app design language
   - Calls useAuthUser().signInWithGoogle() on click
   - Shows loading state while OAuth redirect is processing
   - Dismissible — close button and backdrop click to dismiss
   - Lightweight feel per user decision: not a full-page takeover, just a centered card overlay
   - Props: isOpen, onClose
   - After successful sign-in (detected via useAuthUser().isAuthenticated becoming true), close modal and show toast: "Welcome! Your rankings are saved." using the existing use-toast.ts hook
   - Use framer-motion for enter/exit animations (project standard)

2. Create `src/components/auth/AuthPrompt.tsx`:
   - Small dismissible banner/card shown after first completed ranking
   - Text: "Sign up to save this to your profile" or similar encouraging copy
   - "Continue with Google" button that opens AuthModal
   - "Not now" dismiss button
   - Track dismissal in localStorage so it doesn't re-appear in the same session
   - Props: onDismiss (optional callback)

3. Create `src/components/auth/UserMenu.tsx`:
   - Shows when useAuthUser().isAuthenticated is true
   - Displays user avatar (from user.user_metadata.avatar_url or fallback initial)
   - Dropdown on click with "Sign out" option
   - Sign out calls useAuthUser().signOut(), which returns user to guest state
   - Use Radix DropdownMenu if available in project, otherwise simple dropdown

4. Create `src/components/auth/index.ts` — barrel export for all three components.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AuthModal renders a Google sign-in overlay. AuthPrompt renders a dismissible nudge. UserMenu renders avatar dropdown with sign-out. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth components into layout and match completion flow</name>
  <files>
    src/app/layout.tsx,
    src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx
  </files>
  <action>
1. Update `src/app/layout.tsx` (or the appropriate header/nav component):
   - Import UserMenu and AuthModal
   - In the header/nav area: if useAuthUser().isAuthenticated, show UserMenu. Otherwise, show a "Sign in" button that opens AuthModal.
   - Render AuthModal at root level (controlled by state)
   - NOTE: If the header is in a separate component (e.g., a Nav or Header component), wire it there instead of layout.tsx. Check the existing layout structure first.

2. Wire AuthPrompt into the match completion flow:
   - Find where the completion state is shown (likely in SimpleMatchGrid or the CompletionModal from Phase 1)
   - After a ranking is completed AND useAuthUser().isGuest is true, show AuthPrompt
   - Only show once per session (check localStorage flag)
   - AuthPrompt should appear below or alongside the completion celebration, not blocking it

3. Ensure the toast notification works:
   - After successful Google sign-in, the "Welcome! Your rankings are saved." toast should appear
   - This should be triggered in AuthModal when isAuthenticated transitions from false to true
   - Use existing use-toast.ts hook
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Header shows Sign in button for guests and UserMenu for authenticated users. AuthPrompt appears after first completed ranking for guests. Welcome toast fires after sign-up. App compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Supabase Auth integration: Google OAuth sign-in modal, guest-to-user merge, session persistence, sign-out flow, and post-completion auth prompt.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app
    2. Verify you can browse and rank as a guest (no sign-in required) — confirms AUTH-01
    3. Click "Sign in" in the header — verify the auth modal appears with "Continue with Google" button
    4. Click "Continue with Google" — complete Google OAuth flow — confirms AUTH-03
    5. After sign-in: verify "Welcome! Your rankings are saved." toast appears
    6. Verify your avatar appears in the header with a dropdown containing "Sign out"
    7. Refresh the page — verify you're still signed in (session persists) — confirms AUTH-04
    8. Complete a ranking while signed in — verify it saves correctly
    9. Sign out via avatar dropdown — verify you return to guest state
    10. As a fresh guest, complete a ranking, then sign up — verify the guest ranking is linked to your new account — confirms AUTH-05
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Auth modal opens from header Sign in button
- Google OAuth completes and session is established
- Session persists across page refresh
- UserMenu shows avatar and sign-out works
- Post-completion AuthPrompt shows for guests, is dismissible
- Welcome toast appears after sign-up
</verification>

<success_criteria>
- Guest users can rank freely without any auth gates (AUTH-01)
- Google OAuth sign-in works via modal overlay (AUTH-03, satisfies AUTH-02 per user override)
- Session persists across browser refresh (AUTH-04)
- Guest rankings merge to user account on sign-up (AUTH-05)
- Header shows appropriate auth state (Sign in button vs UserMenu)
- Post-completion nudge appears once for guests and is dismissible
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-migration/02-02-SUMMARY.md`
</output>
