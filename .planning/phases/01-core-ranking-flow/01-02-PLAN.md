---
phase: 01-core-ranking-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/app/modals/completion/CompletionModal.tsx
  - src/components/app/modals/completion/CompletionModalActions.tsx
  - src/components/app/modals/completion/CompletionModalContent.tsx
  - src/components/app/modals/completion/CompletionModalHeader.tsx
  - src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx
  - src/app/features/Landing/LandingMain.tsx
  - src/app/features/Landing/sub_LandingLists/FeaturedListsSection.tsx
  - src/app/features/Landing/sub_LandingLists/SearchFilterBar.tsx
  - src/app/features/Landing/GlobalSearchBar.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - FLOW-03
  - FLOW-04
  - FLOW-06
  - FLOW-07

must_haves:
  truths:
    - "User can complete a full ranking (all grid positions filled) and see a completion modal"
    - "Completion modal shows 4 actions: Download result image, Share link, Keep editing, Start new ranking"
    - "Keep editing dismisses the modal and returns to the grid"
    - "Start new ranking navigates back to landing page"
    - "Drag-and-drop feels smooth with no lag or visual glitches"
    - "User can browse available lists by category on the landing page"
    - "Categories with insufficient items are shown but disabled with Coming soon tag"
    - "User can search for specific lists from the landing page via GlobalSearchBar"
  artifacts:
    - path: "src/components/app/modals/completion/CompletionModalActions.tsx"
      provides: "4-action completion modal (Download stub, Share stub, Keep editing, Start new)"
      contains: "Keep editing"
    - path: "src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx"
      provides: "Completion detection that auto-shows modal when grid is full"
      contains: "isComplete"
    - path: "src/app/features/Landing/LandingMain.tsx"
      provides: "Featured + browse layout with category sections"
      contains: "Coming soon"
    - path: "src/app/features/Landing/GlobalSearchBar.tsx"
      provides: "Search bar for finding specific lists on the landing page"
      contains: "search"
  key_links:
    - from: "src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx"
      to: "src/components/app/modals/completion/CompletionModal.tsx"
      via: "useEffect watching isComplete -> setShowCompletionModal(true)"
      pattern: "isComplete.*CompletionModal"
    - from: "src/app/features/Landing/LandingMain.tsx"
      to: "/api/top/groups"
      via: "Category data fetch for browse sections"
      pattern: "top.*groups|categories"
    - from: "src/app/features/Landing/LandingMain.tsx"
      to: "src/app/features/Landing/GlobalSearchBar.tsx"
      via: "GlobalSearchBar rendered in hero section with search wired to client-side list filtering or /api/search"
      pattern: "GlobalSearchBar"
---

<objective>
Wire the CompletionModal to auto-show when a ranking is complete, update its actions to match the user's 4-action specification, restore GlobalSearchBar for landing page search, and build the landing page browse-by-category experience.

Purpose: With the item loading fixed in Plan 01, this plan completes the remaining Phase 1 requirements -- the completion experience that rewards users for finishing a ranking, and the landing page that lets users discover and search for lists to rank.

Output: Working completion flow with 4 actions, landing page with featured/browse/search via GlobalSearchBar.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-ranking-flow/01-RESEARCH.md
@.planning/phases/01-core-ranking-flow/01-01-SUMMARY.md

@src/components/app/modals/completion/CompletionModal.tsx
@src/components/app/modals/completion/CompletionModalActions.tsx
@src/components/app/modals/completion/CompletionModalContent.tsx
@src/components/app/modals/completion/CompletionModalHeader.tsx
@src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx
@src/app/features/Landing/LandingMain.tsx
@src/app/features/Landing/LandingLayout.tsx
@src/app/features/Landing/sub_LandingLists/FeaturedListsSection.tsx
@src/app/features/Landing/sub_LandingLists/SearchFilterBar.tsx
@src/stores/grid-store.ts

<interfaces>
<!-- Key types from Plan 01 output that this plan uses -->

From src/stores/grid-store.ts (post Plan 01 fix):
```typescript
export function useIsGridComplete(): boolean;
export function useGridStatistics(): GridStatistics;
// GridStatistics.isComplete = matchedCount === total && total > 0
```

From src/components/app/modals/completion/CompletionModal.tsx:
```typescript
export function CompletionModal({
  isOpen, onClose, listTitle, completionData
}: CompletionModalProps)
```

From src/components/app/modals/completion/CompletionModalActions.tsx:
```typescript
// Currently has 3 actions: Tweet, Export (image), Save (stub)
// Needs rewiring to: Download result image (stub), Share link (stub), Keep editing, Start new ranking
export function CompletionModalActions({ onClose }: { onClose: () => void })
```

From src/app/features/Landing/sub_LandingLists/FeaturedListsSection.tsx:
```typescript
// Existing component that shows featured lists
// Needs category browse sections added below it
```

From src/app/features/Landing/sub_LandingLists/SearchFilterBar.tsx:
```typescript
export interface SearchFilterResult { query: string; category: string; timePeriod: TimePeriodFilter; size: ListSizeFilter; }
export function SearchFilterBar({ ... })
```

GlobalSearchBar.tsx (deleted in working tree, must be restored):
```typescript
// Was in src/app/features/Landing/GlobalSearchBar.tsx
// User locked decision: "Search via existing GlobalSearchBar should work for finding specific lists"
// Restore from git HEAD: git show HEAD:src/app/features/Landing/GlobalSearchBar.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CompletionModal auto-show and update to 4-action specification</name>
  <files>src/components/app/modals/completion/CompletionModal.tsx, src/components/app/modals/completion/CompletionModalActions.tsx, src/components/app/modals/completion/CompletionModalContent.tsx, src/components/app/modals/completion/CompletionModalHeader.tsx, src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx</files>
  <action>
**Part A: Auto-show CompletionModal on grid completion**

In `src/app/features/Match/sub_MatchGrid/SimpleMatchGrid.tsx` (or whichever component renders the match grid -- find the correct component by tracing from the match page):
1. Import `useIsGridComplete` from grid-store
2. Add state: `const [showCompletionModal, setShowCompletionModal] = useState(false)`
3. Add state: `const [hasUserDismissed, setHasUserDismissed] = useState(false)` -- prevents re-showing after "Keep editing"
4. Add effect:
   ```typescript
   useEffect(() => {
     if (isComplete && !hasUserDismissed) {
       setShowCompletionModal(true);
     }
   }, [isComplete, hasUserDismissed]);
   ```
5. Render `<CompletionModal>` conditionally when `showCompletionModal` is true
6. Pass props: `isOpen={showCompletionModal}`, `onClose` handler, list title from match store or list store

**Part B: Update CompletionModalActions to 4-action specification**

In `src/components/app/modals/completion/CompletionModalActions.tsx`:
Replace the current 3 actions (Tweet, Export, Save) with 4 actions per user decision:

1. **Download result image** -- visually present button with a download icon, but clicking shows a tooltip or small message "Coming soon -- Phase 4". Use a disabled-looking style or show a brief toast. Do NOT implement actual image download.
2. **Share link** -- visually present button with a share/link icon, same "Coming soon" treatment as above.
3. **Keep editing** -- active button that calls `onClose()` to dismiss the modal. This sets `hasUserDismissed = true` in the parent so the modal does not re-trigger.
4. **Start new ranking** -- active button that navigates to `/` (landing page) using `useRouter().push('/')` from next/navigation.

Layout: Use a 2x2 grid or vertical stack. The "Keep editing" and "Start new ranking" buttons should be visually primary (fully styled). "Download" and "Share" should be present but visually indicate they are upcoming features.

Follow the project's compact UI design skill for button styling (cyan gradient for primary actions, ghost style for secondary).

**Part C: Update CompletionModalHeader and CompletionModalContent if needed**

The header should feel celebratory per user's specific idea. Ensure the header has a congratulatory message (e.g., "Ranking Complete!" with a trophy icon or confetti animation using existing Framer Motion patterns). The content should show a summary of the completed ranking.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - CompletionModal auto-shows when all grid positions are filled
    - Modal shows 4 actions: Download (stub), Share (stub), Keep editing, Start new ranking
    - "Keep editing" dismisses modal and returns to grid; modal does not re-show
    - "Start new ranking" navigates to landing page
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing page category browse and GlobalSearchBar search</name>
  <files>src/app/features/Landing/LandingMain.tsx, src/app/features/Landing/sub_LandingLists/FeaturedListsSection.tsx, src/app/features/Landing/GlobalSearchBar.tsx, src/app/page.tsx</files>
  <action>
**Part A: Category browse sections on landing page**

In `src/app/features/Landing/LandingMain.tsx` (or the appropriate landing layout component):

1. Fetch category data from `/api/top/groups` (or use the existing TanStack Query hook if one exists for top groups). This returns the list of categories with their item counts.

2. Below the existing hero/featured section, add a "Browse by Category" section:
   - Section header: "Browse by Category" with SectionHeader component from sub_LandingLists
   - Grid of category cards (responsive: 2 cols mobile, 3 cols tablet, 4 cols desktop)
   - Each card shows: category name, item count, representative image (if available)
   - Categories with sufficient items (use a threshold -- start with 20 items minimum, adjust based on actual data from the API response) are clickable and navigate to the list selection for that category
   - Categories below the threshold are shown but disabled with a "Coming soon" tag per user decision. They should NOT be hidden. Style them with reduced opacity and a "Coming soon" badge (use the semantic color pattern: `text-gray-500`, `bg-gray-800/40`)

3. The threshold value should be a constant at the top of the component (e.g., `MIN_CATEGORY_ITEMS = 20`) so it can be easily tuned after seeing actual database data.

**Part B: Restore and wire GlobalSearchBar on landing page**

Per user locked decision: "Search via existing GlobalSearchBar should work for finding specific lists."

1. Restore `src/app/features/Landing/GlobalSearchBar.tsx` from git HEAD:
   ```bash
   git show HEAD:src/app/features/Landing/GlobalSearchBar.tsx > src/app/features/Landing/GlobalSearchBar.tsx
   ```

2. After restoring, review the file for any imports that may have broken due to other working-tree changes. Fix any broken imports so the component compiles.

3. Ensure GlobalSearchBar is rendered on the landing page in a prominent position (hero section or just below it). Check where it was previously rendered by searching for `GlobalSearchBar` in `LandingMain.tsx` or `LandingLayout.tsx` in the git history:
   ```bash
   git show HEAD:src/app/features/Landing/LandingMain.tsx | grep -n "GlobalSearchBar"
   ```
   Restore the import and rendering in the same location it was previously used.

4. Verify that GlobalSearchBar's search functionality works -- it should allow typing a list name and showing matching results. The component already has this logic (it was a working feature before deletion).

5. Do NOT use CommandPalette as a replacement for GlobalSearchBar. The CommandPalette may remain as a supplementary keyboard shortcut (Cmd+K) but GlobalSearchBar must be the primary visible search entry point on the landing page per user decision.

**Part C: Featured lists in hero section**

Verify `FeaturedListsSection` displays populated categories prominently in the hero area. The featured lists should showcase the best-populated categories to give a strong first impression per user's specific idea. If the component already does this, leave it. If it shows a static or random selection, update it to prioritize categories with the most items.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Landing page shows featured/popular lists in a hero section
    - GlobalSearchBar is visible and functional on the landing page (per user locked decision)
    - Searching for a list name in GlobalSearchBar returns relevant results
    - Below the hero, categories are displayed in a browse grid
    - Categories with enough items are clickable and navigate to ranking
    - Categories below threshold show "Coming soon" tag but are not hidden
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 end-to-end ranking flow</name>
  <action>
Human verification of the complete Phase 1 ranking flow: item loading, drag-and-drop smoothness, completion modal, session persistence, landing page browse and search.
  </action>
  <what-built>Complete Phase 1 end-to-end ranking flow: item loading, drag-and-drop, completion modal, session persistence, landing page browse and search via GlobalSearchBar</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000 -- verify landing page shows featured lists and category browse sections
    2. Verify "Coming soon" categories are visible but disabled
    3. Verify GlobalSearchBar is visible on the landing page -- type a list name and verify matching results appear (FLOW-07)
    4. Click a list to start ranking -- verify backlog items appear in the collection panel (FLOW-01)
    5. Drag 3-4 items from backlog to grid positions -- verify they land correctly with smooth animation, no lag or visual glitches (FLOW-02, FLOW-04)
    6. Fill ALL grid positions to trigger completion -- verify CompletionModal auto-shows (FLOW-03)
    7. Verify modal shows: Download result image (stub), Share link (stub), Keep editing, Start new ranking
    8. Click "Keep editing" -- verify modal dismisses and does not re-appear
    9. Click "Start new ranking" -- verify navigation to landing page
    10. Close browser tab, reopen, navigate to the same list -- verify grid state is restored (FLOW-05)
    11. Open match-test page in production build (npm run build and npm start, then visit /match-test) -- verify it returns 404
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds
2. Full ranking flow works end-to-end: browse -> search -> select -> rank -> complete -> save -> resume
3. Landing page shows categories with browse and GlobalSearchBar search
4. CompletionModal shows correct 4 actions
5. Drag-and-drop feels smooth with no lag or visual glitches (FLOW-04 verified in checkpoint)
6. Session persistence survives browser close/reopen
</verification>

<success_criteria>
- All 7 FLOW requirements verified through the checkpoint
- CompletionModal auto-shows with correct 4-action layout
- Landing page has featured, browse-by-category, and GlobalSearchBar search
- "Coming soon" categories are visible but disabled
- Drag smoothness confirmed manually (FLOW-04)
- No TypeScript build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-ranking-flow/01-02-SUMMARY.md`
</output>
