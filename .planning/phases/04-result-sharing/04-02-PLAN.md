---
phase: 04-result-sharing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/share/[code]/page.tsx
  - src/app/share/[code]/layout.tsx
  - src/app/api/share/route.ts
  - src/app/api/og/[listId]/route.tsx
autonomous: true
requirements: [SHAR-03, SHAR-04]

must_haves:
  truths:
    - "User can get a unique shareable URL for their completed ranking"
    - "Shared link shows OG preview image when pasted in social media or messaging"
    - "Share page shows result image and 'Challenge it' CTA"
    - "Attribution shows display name if logged in, 'Someone' if guest"
    - "Challenge CTA shows list preview first with 'Start Ranking' button"
  artifacts:
    - path: "src/app/share/[code]/page.tsx"
      provides: "Enhanced share page with result image, challenge CTA, attribution"
      contains: "Challenge"
    - path: "src/app/share/[code]/layout.tsx"
      provides: "OG metadata with top 3 items and cover images"
      contains: "og:image"
    - path: "src/app/api/share/route.ts"
      provides: "Share creation with display_name denormalization"
      contains: "display_name"
  key_links:
    - from: "src/app/share/[code]/page.tsx"
      to: "/api/share/[code]"
      via: "fetch to load shared ranking"
      pattern: "fetch.*api/share"
    - from: "src/app/share/[code]/layout.tsx"
      to: "/api/og/[listId]"
      via: "og:image meta tag URL"
      pattern: "og:image"
    - from: "src/app/share/[code]/page.tsx"
      to: "router.push"
      via: "Challenge CTA navigation"
      pattern: "handleChallenge"
---

<objective>
Enhance the share page and OG preview so shared links look great when pasted in Twitter, iMessage, Slack, and the share page itself feels like receiving a challenge.

Purpose: Closes the viral loop — when someone shares their ranking, the recipient sees a rich preview and is invited to challenge it. Without this, share links are dead ends.
Output: Enhanced /share/[code] page with result image, attribution, challenge CTA; OG preview card showing top 3 items.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-result-sharing/04-CONTEXT.md
@.planning/phases/04-result-sharing/04-RESEARCH.md

<interfaces>
<!-- Key types from share system -->

From src/types/share.ts (SharedRanking type used in page.tsx):
```typescript
export interface SharedRanking {
  id: string;
  share_code: string;
  list_id: string;
  user_id?: string;
  title: string;
  category: string;
  subcategory?: string;
  time_period?: string;
  items: SharedRankingItem[];
  view_count: number;
  challenge_count: number;
  created_at: string;
}

export interface SharedRankingItem {
  position: number;
  title: string;
  description?: string;
  image_url?: string;
}

export interface CreateSharedRankingRequest {
  list_id: string;
  user_id?: string;
  title: string;
  category: string;
  subcategory?: string;
  time_period?: string;
  items: SharedRankingItem[];
}
```

From src/app/share/[code]/layout.tsx (existing OG metadata generation):
- Already generates og:title, og:description, og:image
- Uses suggestLayout() to pick card layout
- Points og:image at /api/og/[listId]

From src/app/api/share/route.ts:
- POST handler creates shared ranking with share_code
- Returns { success, data: { share_url, share_code } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add display_name to share creation and enhance OG metadata</name>
  <files>src/app/api/share/route.ts, src/app/share/[code]/layout.tsx</files>
  <action>
1. In `/api/share/route.ts` POST handler:
   - Accept optional `display_name` field in the request body
   - Store `display_name` in the shared_rankings row (denormalized — avoids join on every view)
   - If no display_name provided, store null (guest user)
   - Add `display_name` to the CreateSharedRankingRequest interface in src/types/share.ts

2. In `/api/share/[code]/route.ts` GET handler:
   - Ensure `display_name` is returned in the SharedRanking response
   - Add `display_name` to the SharedRanking interface in src/types/share.ts

3. In `src/app/share/[code]/layout.tsx`:
   - Ensure the og:description includes attribution: "{display_name}'s Top {N} {category}" or "Someone ranked their Top {N} {category}"
   - Ensure og:image URL points to `/api/og/{listId}` with query params for the share data (title, top 3 item names, category)
   - Verify og:image dimensions are set: `<meta property="og:image:width" content="1200" />` and height="630"
   - Ensure twitter:card is set to "summary_large_image" for rich preview on Twitter
   - Ensure twitter:image is also set

4. In `/api/og/[listId]/route.tsx`:
   - Verify the OG card renders the "featured" layout showing top 3 items with cover images + list title (this is the user decision for OG preview)
   - If the featured layout doesn't show item images, update it to include up to 3 item thumbnail images from the shared ranking items' image_url field
   - Ensure the response has correct Cache-Control headers (cache for 1 hour minimum)
  </action>
  <verify>
    <automated>cd C:/Users/kazim/dac/goat && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - display_name stored at share creation time (denormalized)
    - OG metadata includes attribution in description
    - twitter:card set to summary_large_image
    - OG image shows top 3 items with cover images
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance share page with result image, attribution, and challenge CTA</name>
  <files>src/app/share/[code]/page.tsx</files>
  <action>
Refactor the share page to match user decisions: result image + "Challenge it" CTA that feels like receiving a challenge.

1. Attribution header:
   - If `ranking.display_name` exists: "{display_name}'s Top {N} {category}" (e.g., "kazim's Top 10 RPGs")
   - If no display_name: "Someone ranked their Top {N} {category}" (e.g., "Someone ranked their Top 10 RPGs")
   - Use the display_name field added in Task 1

2. Result display:
   - Show a styled card with the full ranking list (all items, not just top 3)
   - Each item shows: position number, thumbnail (if image_url exists), title
   - Use theme-consistent styling (dark bg, brand colors matching the app aesthetic)
   - The existing ShareableListCard component can be used if it renders well, otherwise build a simpler inline list

3. Challenge CTA section (per user decisions):
   - "Think you can do better?" text above button
   - "Make Your Own" / "Start Ranking" button with gradient background (brand gradient: #06b6d4 -> #8b5cf6)
   - On click: show a brief preview of the list details (category, item count, description) BEFORE navigating
   - Use a small expandable section or modal that shows: list title, category, number of items, then "Start Ranking" button that navigates to the list
   - This is the "same list but show preview first" user decision — one extra click but sets expectations

4. Social share row:
   - Keep existing social platform buttons (Twitter, Facebook, Reddit, WhatsApp, Discord, LinkedIn)
   - Keep the copy link button
   - Keep native share button for mobile

5. Responsive layout:
   - On mobile (< 768px): single column, full-width cards
   - On desktop: centered max-w-2xl layout
   - Ensure touch targets are 44px minimum for mobile accessibility

6. Remove the handleChallenge API call that POSTs to `/api/share/${code}` — instead navigate directly to the list page (or landing page with list pre-selected). The challenge is about creating your own ranking of the same topic, not an API action.
  </action>
  <verify>
    <automated>cd C:/Users/kazim/dac/goat && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Share page shows attribution (display name or "Someone")
    - Full ranking displayed with position numbers and thumbnails
    - "Challenge it" CTA shows list preview before navigating
    - Social share buttons functional
    - Responsive layout works on mobile widths
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Create a shared ranking via the share API, visit /share/[code]
3. Verify attribution shows correctly for logged-in and guest users
4. Verify challenge CTA shows list preview before navigation
5. Check OG meta tags in page source (view-source or curl)
6. Test social share buttons open correct URLs
</verification>

<success_criteria>
- Share page shows ranked items with thumbnails and attribution
- Challenge CTA previews list before navigation
- OG meta tags render correctly with top 3 items, attribution, summary_large_image
- display_name denormalized at share creation time
- Page responsive down to 375px width
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-sharing/04-02-SUMMARY.md`
</output>
