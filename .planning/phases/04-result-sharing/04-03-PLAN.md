---
phase: 04-result-sharing
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/app/features/Match/sub_MatchGrid/components/GridRenderer.tsx
  - src/app/features/Match/sub_MatchGrid/components/GridSection.tsx
  - src/app/features/Match/sub_MatchCollections/SimpleCollectionPanel.tsx
  - src/app/features/Match/sub_MatchCollections/components/MobileBacklogPanel.tsx
  - src/stores/grid-store.ts
autonomous: false
requirements: [MOBL-01, MOBL-02]

must_haves:
  truths:
    - "Ranking grid is usable on mobile devices with tap-to-place interaction"
    - "Backlog appears as collapsible bottom panel on mobile (< 768px)"
    - "User can tap a backlog item then tap an empty grid slot to place it"
    - "User can long-press a grid item to pick it up and drag to swap"
    - "Result and sharing pages render correctly on mobile"
  artifacts:
    - path: "src/app/features/Match/sub_MatchCollections/components/MobileBacklogPanel.tsx"
      provides: "Collapsible bottom sheet for backlog on mobile"
      contains: "bottom"
    - path: "src/app/features/Match/sub_MatchGrid/components/GridRenderer.tsx"
      provides: "Compact grid cards, tap-to-place handler, and long-press-to-drag on mobile"
      contains: "mobileSelection"
    - path: "src/stores/grid-store.ts"
      provides: "Mobile selection state for tap-to-place"
      contains: "mobileSelectedItem"
  key_links:
    - from: "MobileBacklogPanel.tsx"
      to: "grid-store"
      via: "mobileSelectedItem state for tap-to-place"
      pattern: "mobileSelectedItem"
    - from: "GridRenderer.tsx"
      to: "grid-store"
      via: "handleMobileTapSlot places selected item"
      pattern: "handleMobileTapSlot"
    - from: "SimpleCollectionPanel.tsx"
      to: "MobileBacklogPanel.tsx"
      via: "renders MobileBacklogPanel below md breakpoint"
      pattern: "MobileBacklogPanel"
    - from: "GridRenderer.tsx"
      to: "@dnd-kit TouchSensor"
      via: "long-press activates drag for grid item reorder"
      pattern: "TouchSensor.*delay"
---

<objective>
Make the ranking grid and sharing pages fully usable on mobile devices with touch-native interactions.

Purpose: Mobile is where sharing happens most — if the grid is unusable on phones, the viral loop breaks. Tap-to-place replaces drag-and-drop for item placement, and a bottom sheet replaces the sidebar for the backlog.
Output: Mobile-responsive grid with tap-to-place, long-press-to-drag reorder, bottom panel backlog, compact cards, and responsive share pages.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-result-sharing/04-CONTEXT.md
@.planning/phases/04-result-sharing/04-RESEARCH.md
@.planning/phases/04-result-sharing/04-01-SUMMARY.md
@.planning/phases/04-result-sharing/04-02-SUMMARY.md

<interfaces>
<!-- Key interfaces for grid and collection components -->

From src/stores/grid-store.ts:
```typescript
// Grid items array with 50 max positions
gridItems: GridItem[]
// Completion detection
isComplete: boolean // matchedCount === total && total > 0
// Drag handlers
handleDragEnd: (event: DragEndEvent) => void
```

From src/app/features/Match/sub_MatchCollections/SimpleCollectionPanel.tsx:
- Renders backlog items in a sidebar panel
- Used on desktop as a side panel alongside the grid

From src/app/features/Match/hooks/useTouchGestures.ts:
- Existing touch gesture hook that can be leveraged for long-press detection
- Already handles touch start/move/end events

From src/app/features/Match/sub_MatchGrid/lib/hapticFeedback.ts:
- hapticFeedback utilities for vibration feedback on mobile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile selection state, compact grid layout, and long-press-to-drag reorder</name>
  <files>src/stores/grid-store.ts, src/app/features/Match/sub_MatchGrid/components/GridRenderer.tsx, src/app/features/Match/sub_MatchGrid/components/GridSection.tsx</files>
  <action>
1. In grid-store.ts, add mobile tap-to-place state:
   ```typescript
   mobileSelectedItem: { id: string; title: string; image_url?: string } | null
   setMobileSelectedItem: (item: { id: string; title: string; image_url?: string } | null) => void
   handleMobileTapSlot: (position: number) => void
   ```
   - `setMobileSelectedItem`: sets the currently selected backlog item for tap-to-place
   - `handleMobileTapSlot`: if mobileSelectedItem is set, place it at the given position (reuse existing assign logic from handleDragEnd), then clear mobileSelectedItem. If position is already occupied, do nothing (or swap if the user has an item selected).
   - Clear `mobileSelectedItem` on any drag start (to prevent state conflicts between tap-to-place and long-press-to-drag)

2. In GridRenderer.tsx (or the component that renders individual grid slots):
   - Detect mobile via `useMediaQuery('(max-width: 767px)')` or `window.matchMedia`
   - On mobile, add an `onClick` handler to empty grid slots that calls `handleMobileTapSlot(position)`
   - On mobile, show a highlight ring on the slot when `mobileSelectedItem` is set (indicating "tap here to place")
   - Compact card sizing on mobile: reduce card dimensions to ~64px height, show only thumbnail + truncated title
   - Reduce grid gap and padding on mobile

3. In GridSection.tsx (or equivalent grid layout component):
   - Add responsive Tailwind classes for compact mobile layout:
     - `gap-1 sm:gap-2 lg:gap-3`
     - Cards: `h-16 sm:h-20 lg:h-24`
   - Ensure grid is scrollable on mobile (overflow-y-auto with max-height)
   - Apply `text-xs` for position numbers and titles on mobile

4. Add haptic feedback on successful placement:
   - Import hapticFeedback utility
   - Trigger short vibration when item is placed via tap

5. Wire long-press-to-drag reorder on mobile grid items (per user decision: "long-press to pick up, drag to swap"):
   - In the DndContext setup (GridRenderer.tsx or parent component that wraps the grid with DndContext), configure sensors to include @dnd-kit's `TouchSensor` with delay-based activation:
     ```typescript
     import { TouchSensor, useSensor, useSensors } from '@dnd-kit/core';
     const touchSensor = useSensor(TouchSensor, {
       activationConstraint: { delay: 350, tolerance: 5 }
     });
     ```
   - This makes occupied grid items draggable after a 350ms long-press hold, matching standard mobile reorder UX (like iOS home screen)
   - Import `useTouchGestures` from `src/app/features/Match/hooks/useTouchGestures.ts` for supplemental gesture feedback (visual scale-up on long-press start)
   - On each occupied grid slot (mobile only), add a visual indicator when long-press is detected: slight scale(1.05) + subtle shadow lift to signal the item is "picked up"
   - On drag end between two occupied slots, perform a swap using existing grid-store handleDragEnd swap logic
   - Trigger haptic feedback on long-press activation (medium vibration) and on successful swap (short vibration)
   - Clear `mobileSelectedItem` on any drag start to prevent state conflicts between tap-to-place and long-press-to-drag
  </action>
  <verify>
    <automated>cd C:/Users/kazim/dac/goat && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Grid store has mobileSelectedItem state and handleMobileTapSlot action
    - Grid slots respond to tap on mobile to place selected item
    - Cards are compact on mobile (64px height, thumbnail + truncated title)
    - Grid has reduced gaps and padding on mobile
    - Long-press (350ms) on occupied grid items activates drag mode for reorder/swap
    - TouchSensor configured with delay activation constraint
    - Haptic feedback on placement and on long-press drag activation
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create mobile bottom panel for backlog</name>
  <files>src/app/features/Match/sub_MatchCollections/components/MobileBacklogPanel.tsx, src/app/features/Match/sub_MatchCollections/SimpleCollectionPanel.tsx</files>
  <action>
1. Create new component `MobileBacklogPanel.tsx`:
   - Framer Motion bottom sheet with three states:
     - Collapsed: 80px peek bar showing "Backlog ({count} items)" + drag handle
     - Half-expanded: 50vh showing scrollable item grid
     - Full-expanded: 85vh (leave space for grid peek above)
   - Drag handle at top (12px wide, 4px tall, rounded, centered)
   - Use Framer Motion `drag="y"` with constraints:
     ```typescript
     dragConstraints={{ top: -(expandedHeight - COLLAPSED_HEIGHT), bottom: 0 }}
     dragElastic={0.1}
     ```
   - Snap logic on dragEnd:
     - Velocity > 500 downward: collapse
     - Velocity > 500 upward: expand to half
     - Otherwise: snap to nearest position based on offset
   - Content: render backlog items in a 3-column grid
   - Each backlog item is tappable — on tap:
     a. Call `setMobileSelectedItem({ id, title, image_url })` from grid-store
     b. Highlight the selected item with a ring/border
     c. Auto-collapse to peek bar so user can see the grid and tap a slot
   - Apply `touch-action: none` on the drag handle
   - Apply `overscroll-behavior: contain` on the scrollable content area
   - Apply `overflow: hidden` on body when panel is expanded past half (prevent background scroll)
   - Use `position: fixed; bottom: 0; left: 0; right: 0; z-index: 40`

2. In SimpleCollectionPanel.tsx:
   - Detect mobile breakpoint using `useMediaQuery('(max-width: 767px)')` or equivalent
   - On mobile (< 768px): render MobileBacklogPanel instead of the sidebar
   - On desktop (>= 768px): render existing sidebar as-is
   - Pass the same backlog items, search/filter state, and handlers to MobileBacklogPanel

3. Styling details:
   - Panel background: `bg-gray-900 rounded-t-2xl` with subtle shadow upward
   - Item cards in panel: thumbnail (48px), title truncated, tap highlight with brand color ring
   - Selected item: ring-2 ring-brand-hover with slight scale-105
   - Deselect: tap same item again, or tap a grid slot (placement clears selection)
  </action>
  <verify>
    <automated>cd C:/Users/kazim/dac/goat && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MobileBacklogPanel renders as bottom sheet on mobile
    - Three states: collapsed peek, half-expanded, full-expanded
    - Backlog items tappable for selection with visual highlight
    - Selection auto-collapses panel so grid is visible
    - Body scroll locked when panel expanded
    - Desktop sidebar unchanged
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify mobile grid and sharing pages</name>
  <what-built>
    Mobile-responsive ranking grid with tap-to-place interaction, long-press-to-drag reorder, collapsible bottom panel for backlog, compact grid cards, and responsive share pages from Plan 02.
  </what-built>
  <how-to-verify>
    1. Open dev server at http://localhost:3000
    2. Open browser DevTools and switch to mobile viewport (iPhone 14 Pro, 393x852)
    3. Navigate to a list and start ranking
    4. Verify the backlog appears as a bottom panel (not a sidebar)
    5. Drag the bottom panel handle up — verify it expands to half-screen
    6. Tap a backlog item — verify it highlights and panel collapses
    7. Tap an empty grid slot — verify the item is placed there
    8. Verify grid cards are compact (small thumbnails, truncated titles)
    9. Long-press a placed grid item (~350ms) — verify it enters drag mode for reorder
    10. Drag the picked-up item to another occupied slot — verify they swap
    11. Complete the grid — verify pulsing Share button appears in header
    12. Open share modal — verify it works on mobile viewport
    13. Visit /share/[code] on mobile viewport — verify page renders correctly
    14. Check that social buttons and challenge CTA are tappable (44px+ touch targets)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Mobile viewport: grid has compact cards, bottom panel for backlog
3. Tap-to-place: select item from panel, tap grid slot, item placed
4. Long-press-to-drag: pick up grid item, drag to swap positions
5. Share pages responsive on 375px and 393px widths
6. Touch targets >= 44px on interactive elements
</verification>

<success_criteria>
- Grid usable on mobile with tap-to-place (no drag required for initial placement)
- Bottom panel replaces sidebar on mobile with collapse/expand/peek states
- Compact cards show thumbnail + title only on mobile
- Long-press enables drag-to-reorder on mobile via @dnd-kit TouchSensor with delay constraint
- Share page and share modal render correctly on mobile viewports
- No scroll conflicts between bottom panel and page content
</success_criteria>

<output>
After completion, create `.planning/phases/04-result-sharing/04-03-SUMMARY.md`
</output>
