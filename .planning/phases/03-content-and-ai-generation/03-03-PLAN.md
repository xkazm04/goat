---
phase: 03-content-and-ai-generation
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/app/features/Studio/components/StudioFormPanel.tsx
  - src/app/features/Studio/components/StudioItemCard.tsx
  - src/app/features/Studio/components/StudioError.tsx
  - src/app/features/Studio/components/MetadataPanel.tsx
  - src/app/features/Studio/components/PublishSuccess.tsx
autonomous: false
requirements: [CONT-04]

must_haves:
  truths:
    - "Studio shows clear progress feedback during generation (progress bar or item counter)"
    - "Studio shows actionable error messages when generation fails"
    - "User can edit item titles and remove unwanted items before publishing"
    - "Save flow is two-step: save as draft first, then explicit Publish to make list rankable"
    - "Published list navigates user to the ranking page"
  artifacts:
    - path: "src/app/features/Studio/components/StudioFormPanel.tsx"
      provides: "Generate button with loading state and progress indicator"
      contains: "isGenerating"
    - path: "src/app/features/Studio/components/StudioError.tsx"
      provides: "Error state with retry suggestion"
      min_lines: 20
    - path: "src/app/features/Studio/components/StudioItemCard.tsx"
      provides: "Editable title, remove button per item"
      contains: "updateItem"
    - path: "src/app/features/Studio/components/MetadataPanel.tsx"
      provides: "Draft save + Publish two-step flow"
      contains: "draft|publish"
  key_links:
    - from: "src/app/features/Studio/components/MetadataPanel.tsx"
      to: "/api/studio/save-items"
      via: "publish action saves items then creates list"
      pattern: "save-items|publish"
    - from: "src/app/features/Studio/components/StudioItemCard.tsx"
      to: "studio-store"
      via: "updateItem and removeItem actions"
      pattern: "updateItem|removeItem"
---

<objective>
Polish the Studio list creation UI with progress feedback, error states, item editing, and a clear draft/publish flow.

Purpose: The generation pipeline works (from Plan 02) but the UI needs to feel polished and intuitive. Users need clear feedback during generation, the ability to curate their list, and a confident save/publish experience.

Output: Polished Studio UI with progress, errors, item editing, and draft/publish flow.
</objective>

<execution_context>
@C:/Users/kazim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kazim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-and-ai-generation/03-02-SUMMARY.md

@src/stores/studio-store.ts
@src/app/features/Studio/StudioLayout.tsx
@src/app/features/Studio/components/StudioFormPanel.tsx
@src/app/features/Studio/components/StudioItemCard.tsx
@src/app/features/Studio/components/StudioError.tsx
@src/app/features/Studio/components/MetadataPanel.tsx
@src/app/features/Studio/components/PublishSuccess.tsx
@src/app/features/Studio/components/TopicInputForm.tsx
@src/app/api/studio/save-items/route.ts
@src/types/studio.ts

<interfaces>
<!-- Store selectors the executor needs -->

From src/stores/studio-store.ts:
```typescript
export const useStudioGeneration = () => ({ isGenerating, generationProgress, error, generateItems, clearError });
export const useStudioItems = () => ({ generatedItems, clearItems, updateItem, removeItem, reorderItems, addItem });
export const useStudioValidation = () => ({ canPublish, hasTitle, hasItems, itemCount, listSize });
export const useStudioPublishing = () => ({ isPublishing, publishError, publishedListId, showSuccess, ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish generation UX -- progress indicator, error states, item editing</name>
  <files>src/app/features/Studio/components/StudioFormPanel.tsx, src/app/features/Studio/components/StudioItemCard.tsx, src/app/features/Studio/components/StudioError.tsx</files>
  <action>
**StudioFormPanel.tsx:**
1. During generation (`isGenerating === true`), replace the generate button text with a progress indicator showing `generationProgress` from the store (e.g., "Loading item 15/30..."). Use a subtle pulsing animation on the button (Tailwind `animate-pulse`).
2. Disable the topic input and generate button during generation.
3. After generation completes, show a brief "items generated" count below the form.

**StudioItemCard.tsx:**
1. Make the item title editable: clicking the title switches to an inline text input. On blur or Enter, call `updateItem(index, { title: newTitle })`.
2. Add a visible "X" remove button in the top-right corner of each card. On click, call `removeItem(index)`. Add a subtle fade-out animation on removal using Framer Motion `AnimatePresence`.
3. Show the item's image using the existing `placeholder-image` component as fallback when `image_url` is null.

**StudioError.tsx:**
1. When `error` is set in the store, show an error banner above the items grid with:
   - The error message text
   - A suggestion: "Try rephrasing your topic or being more specific"
   - A "Dismiss" button that calls `clearError()`
   - A "Try Again" button that calls `generateItems()` again
2. Style with a red/amber border, warning icon, and clear contrast against the dark theme.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Generate button shows progress during generation, item titles are editable inline, items have remove buttons with animation, error banner shows with retry/dismiss actions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement draft/publish two-step save flow</name>
  <files>src/app/features/Studio/components/MetadataPanel.tsx, src/app/features/Studio/components/PublishSuccess.tsx</files>
  <action>
**MetadataPanel.tsx:**
1. Replace the current single publish button with a two-step flow:
   - **"Save Draft"** button: saves the current state to the studio store's persist (already using Zustand). Show a toast "Draft saved" using the existing toast system. The draft button is always available once items exist.
   - **"Publish"** button: calls the existing publish flow (save-items API + create list). Only enabled when `canPublish` validation passes (has title + enough items). Style as the primary action button (more prominent than Save Draft).
2. Show clear validation messages below the Publish button when it's disabled:
   - Missing title: "Add a list title to publish"
   - Not enough items: "Need at least {listSize} items ({itemCount} generated)"
3. During publishing (`isPublishing`), show a spinner on the Publish button and disable both buttons.

**PublishSuccess.tsx:**
1. After successful publish, show the success overlay with:
   - Confetti or celebration animation (use existing `SuccessCelebration` component if available, otherwise simple CSS animation)
   - "Your list is ready to rank!" message
   - "Start Ranking" primary button that navigates to the match page for the new list (`/match?listId={publishedListId}`)
   - "Create Another" secondary button that calls `reset()` on the studio store
2. Ensure the success overlay covers the Studio content area clearly.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Two-step save flow works: Save Draft persists state, Publish creates the list. Validation messages shown when publish requirements not met. Success overlay offers Start Ranking and Create Another actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Studio list creation flow: type a topic, see items appear progressively, edit titles, remove items, save draft, publish, and navigate to ranking.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/studio
    2. Type "Best Horror Games" in the topic field and click Generate
    3. Verify items appear one-by-one with subtle animation (progressive reveal)
    4. Verify progress shows "Loading item X/Y..." during generation
    5. Click an item title and edit it -- verify the change persists
    6. Click the X button on an item -- verify it fades out and is removed
    7. If generation fails, verify error banner appears with "Try Again" button
    8. Click "Save Draft" -- verify toast appears
    9. Fill in list title if needed, click "Publish"
    10. Verify success screen appears with "Start Ranking" button
    11. Navigate to landing page -- verify only populated categories are visible (no "Coming soon" badges)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds: `npx next build`
- Studio flow works end-to-end: generate -> edit -> publish -> rank
- Error states display correctly with retry option
- Landing page shows only populated categories
</verification>

<success_criteria>
- Studio generation shows progress feedback and progressive item reveal
- Items are editable (title) and removable
- Two-step save: draft + publish
- Error states are actionable (retry, dismiss)
- Published list can be ranked immediately
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-and-ai-generation/03-03-SUMMARY.md`
</output>
